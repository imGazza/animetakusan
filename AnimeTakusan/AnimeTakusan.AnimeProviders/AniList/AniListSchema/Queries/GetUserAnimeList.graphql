query GetUserAnimeList($userId: Int) {
  MediaListCollection(userId: $userId, type: ANIME, sort: STATUS) {
    lists {
      name
      entries {
        id
        media{
          ...Anime
        }
      }
    }
  }
}
